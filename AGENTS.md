This is my dotfiles project. The content is symlinked to ~/

Use `devtool` for local build, test, lint, check, and duplicate-code workflows when available.

Tracked config uses explicit `.gitignore` allowlists. When adding config under ignored trees such as `.config/`, add the allowlist entry first and use normal `git add`; do not use `git add -f` as the default path.

## Imported Claude Cowork project instructions

dotfiles contains all my config including for Claude and Codex.

Some skills are symlinks to ~/.agents
