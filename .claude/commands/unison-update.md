# Unison update

This command applies when editing unison code, after an `update`, and the UCM
has created an update branch.

You must repair the contents of the scratch file using the unison skill,
but DO NOT delete functions as that will remove them from the codebase.
